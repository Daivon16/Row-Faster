# Example environment configuration for Row-Faster
# Copy this file to `.env` and fill in your values

# OpenAI API key used by the /coach endpoint
OPENAI_API_KEY=

# Port for the Express server (defaults to 3000 if not set)
PORT=3000
